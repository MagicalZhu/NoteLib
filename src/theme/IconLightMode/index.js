import React from 'react';
import IconLightMode from '@theme-original/IconLightMode';

export default function IconLightModeWrapper(props) {
  return (
    <>
      <IconLightMode {...props} />
    </>
  );
}
